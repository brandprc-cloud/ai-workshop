# AI Workshop — вопросы для уточнения перед ТЗ (23.06.2026)

Сгруппированы по адресату. На основе ответов пишется ТЗ для Клода Арташеса.

---

## А. Насте (SMM / воронки BotHelp)

1. Сколько всего воронок в BotHelp и какой лид-магнит в каждой? (известно про 16 курсов Anthropic; есть инфо, что есть и другие — какие?)
2. По каждой воронке: триггер (коммент под видео / в посте / Директ), ключевые слова, что отвечает бот в Директе (текст + кнопка), куда ведёт (какой бот / канал / ссылка).
3. Точные ссылки/хэндлы всех BotHelp-ботов.
4. Какая аналитика есть в BotHelp (переходы, выдача лид-магнита, клики)? Можно выгрузить или дать доступ?
5. Как лиды из BotHelp попадают в CRM distressfinder — автоматически, вручную, никак?
6. На этом пути есть призыв на воркшоп (анкета/оплата) или только PDF + подписка на канал @artashesgri?
7. Почему воронка сейчас не работает (инцидент 23.06) — что сломалось?
8. На ссылках в IG (bio, посты) есть UTM-метки? Какие?

## Б. Арташесу (CRM / бот / деньги / архитектура)

1. **Источник правды:** дашборд (distressfinder), админ-бот и backend сайта `/api/apply` — одна база или разные? Почему бот показывает 169, а статистика 176?
2. Старые `gform_` в CSV — Google Form это легаси, сейчас всё через `/api/apply`?
3. Статус лида — одно текущее значение или хранится **история переходов с таймстампами**?
4. «Прислал чек 0» при приходящих чеках — стадия проставляется или мёртвая? Где **истина по оплатам**?
5. Кнопка «Финансы» (403 Forbidden) — баг прав или раздел не достроен? **Где деньги сводятся в сумму прихода** (есть ли вообще такое место)?
6. Подтверждение оплаты — всегда только ручной `/confirm`, или по какому-то способу есть авто-сверка?
7. **Дата участия** — как проставляется? Почему у нового лида без оплаты стоит сегодняшняя дата? Почему у 48 выпускников дата потока есть только у 6?
8. Теги гео/язык — присвоенные или кнопки для назначения? Ставятся вручную или из анкеты? Почему у лида показаны все теги сразу?
9. Тестовые записи (`__SPEEDTEST__`) и команда как лиды (@ZakharNN = lead #201) — входят в счётчик 176? Чистятся?
10. Бот @ArtashesAIBot в `/start` показывает прошедшую дату 13–14 июня — можно завязать на единый `cohorts.json` (как на сайте)?
11. **GA4** (`G-KPRNZCDH2E`) — кто владелец, есть доступ, настроены ли цели/конверсии?
12. Что внутри кнопок дашборда **Рефералы / SMM / Видео**?
13. **Ziina-ссылка статическая** (одна на всех) — намеренно? Готов перейти на per-lead ссылки для авто-привязки оплаты?
14. Кто и чем правит базу (бот / дашборд), есть журнал изменений?
15. **Доступ продюсеру** (read-only) к дашборду / GA4 / экспорту — дашь?

## В. Ответится, когда починят бот / технически
- Точный текст и кнопки первого сообщения BotHelp-бота, экран выдачи лид-магнита.
- Полный сценарий клиентского бота после оплаты (что после `/confirm`).
